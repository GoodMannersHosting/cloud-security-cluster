name: keeper-aws-infra
runtime: nodejs
description: AWS IAM for keeper Route53 dnsweaver, ExternalDNS Route53, GitHub Actions OIDC deploy, and Authentik -> IAM Identity Center (SAML + SCIM)
packages:
  authentik:
    source: terraform-provider
    version: 1.2.1
    parameters:
      - goauthentik/authentik
