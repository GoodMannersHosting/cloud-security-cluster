# Files
*.key
*.pem
*kubeconfig*
*talosconfig*

# Directories
node_modules

# VPS stack secrets and ACME store
stacks/*/.env
stacks/**/acme.json
stacks/**/config/local.hcl
bao/config.env

# age secret key for SOPS (Doco-CD); never commit
stacks/doco-cd/sops_age_key.txt

# Plaintext counterparts of committed SOPS files (encrypt → secrets.enc.env then commit)
stacks/**/*.env.plain
stacks/**/secrets.env
stacks/**/secrets.env.env
stacks/**/.decrypted~*

# Off-site backup credentials (S3 IAM user)
stacks/ops/backup.env
stacks/ops/aws/credentials
stacks/ops/aws/keeper.key
stacks/ops/aws/ca.srl
/opt/stack-secrets/keeper-ra-ca.key

# System Files
.DS_Store

# Git worktrees (local feature checkouts)
.worktrees/
.backup-2026-09-13/
